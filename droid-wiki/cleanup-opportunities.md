# Cleanup opportunities

A verified list of rough edges in the codebase, each confirmed against the source. None are urgent; all are good first contributions. Follow the [development workflow](how-to-contribute/development-workflow.md) and [patterns and conventions](how-to-contribute/patterns-and-conventions.md) when picking one up.

| # | Finding | Location | Suggested fix |
| --- | --- | --- | --- |
| 1 | Dead deep-link param: the Explorer "Ask assistant about this" button links to `/app/assistant?document=<id>` (`src/pages/app/ExplorerPage.tsx:98`), but AssistantPage only reads the `prompt` search param (`src/pages/app/AssistantPage.tsx:21`). The `document` param is silently ignored. | `src/pages/app/ExplorerPage.tsx`, `src/pages/app/AssistantPage.tsx` | Read `document` in AssistantPage, fetch the document via `/api/documents/:id`, and seed the input with a question about it; or change the link to pass `prompt`. |
| 2 | Frozen demo clock: the documents date-range filter anchors "now" to the literal `"2024-12-31"` (`src/mocks/handlers.ts`). | `src/mocks/handlers.ts` | Anchor to the newest date in `src/mocks/data.ts` (or `Date.now()` if fixtures are regenerated) so range filters stay meaningful as fixtures change. |
| 3 | Changelog date mismatch: `CHANGELOG.md` dates v0.1.0 as 2025-01-15, but every commit in the repository is dated 2026-09-21. | `CHANGELOG.md` | Correct the release date to match the actual history. |
| 4 | Hardcoded page data: the dashboard activity feed (`src/pages/app/DashboardPage.tsx:12`), the analytics users table (`src/pages/app/AnalyticsPage.tsx:25`), and the auth page stats row (`src/pages/app/AuthPage.tsx:77`) are literals in page components instead of living in `src/mocks/data.ts` or `src/config/brand.ts`. | `src/pages/app/DashboardPage.tsx`, `src/pages/app/AnalyticsPage.tsx`, `src/pages/app/AuthPage.tsx` | Move activity/users to mock fixtures served by MSW endpoints, and marketing stats to the brand config, consistent with the project's data-routing convention. |
| 5 | Unused signup field: the signup form collects "Full name" (`src/pages/app/AuthPage.tsx`), but `login()` in `src/store/auth.ts` ignores it and derives the display name from the email address (`src/store/auth.ts:15`). | `src/pages/app/AuthPage.tsx`, `src/store/auth.ts` | Pass the name through to the auth store on signup, or drop the field. |
| 6 | Dark mode toggle with no dark styles: Settings has a persisted theme toggle (`src/pages/app/SettingsPage.tsx`, `src/store/theme.ts`) and Tailwind is configured with `darkMode: "class"`, but the only `dark:` utility in the app is one button variant in `src/components/ui/button.tsx`. | `src/components/ui/button.tsx`, `tailwind.config.ts`, pages | Either add `dark:` variants across layouts and pages so the toggle works, or remove the toggle until theming is implemented. |
| 7 | Inconsistent Radix wrapping: `Button`, `Card`, `Dialog`, `Input`, and `Badge` have wrappers in `src/components/ui/`, but pages use Radix `Switch` and `Tabs` primitives directly (`src/pages/app/SettingsPage.tsx:1-2`, `src/pages/app/IntegrationsPage.tsx:1`). | `src/pages/app/SettingsPage.tsx`, `src/pages/app/IntegrationsPage.tsx`, `src/components/ui/` | Add `switch.tsx` and `tabs.tsx` wrappers in `src/components/ui/` and migrate the pages, matching the shadcn-style pattern. |
| 8 | Unused dependencies: `@radix-ui/react-dropdown-menu` and `@radix-ui/react-label` are declared in `package.json`, but a grep of `src/` finds no imports of either. | `package.json` | Remove both dependencies (and their lockfile entries), or build the components that need them. |
| 9 | No 404 route: the catch-all route renders the marketing Landing page (`src/App.tsx:38`), so any mistyped URL silently shows the homepage. | `src/App.tsx` | Add a dedicated NotFound page with a link home, keeping the redirect behavior only where intended. |

Each fix should ship with the full five-command gate green; see [how to contribute](how-to-contribute/index.md).
